count = 10;
i = 0;
loop count times:
	simon says i;
	if i modulo 2 == 0:
		print "EVEN";
	else:
		print "ODD";
	end;
	i = i + 1;
loopstop;

print i;
print count;
assert count == 10 : "'count' must be equal to '10' at this point";
assert i == 10 : "'i' must be equal to 'count' at this point";

print "SUCCESS!";