size = 10;
i = 0;
loop size times:
	simon says i;
	if i modulo 2 == 0:
		print "EVEN";
	else:
		print "ODD";
	end;
	i = i + 1;
loopstop;

print i;
print size;
assert size == 10 : "'size' must be equal to '10' at this point";
assert i == 10 : "'i' must be equal to 'size' at this point";

print "SUCCESS!";